{
  "ResetPassword" : {
    "userPoolId" : "User pool ID",
    "userId" : "User ID",
    "newPassword" : "New password (8-32 characters, must contain uppercase, lowercase, and digits)",
    "password" : "Current password; if provided, validates the current password (user self-change scenario); if omitted, admin reset is performed and validation is skipped",
    "forceResetPassword" : "Whether to force the user to reset password on next login; default is true"
  }
}