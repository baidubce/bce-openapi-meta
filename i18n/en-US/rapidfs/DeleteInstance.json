{
  "DeleteInstance" : {
    "action" : "Fixed value: DeleteInstance",
    "clientToken" : "Ensures idempotency of the API call",
    "instanceId" : "The ID of the RapidFS instance to be released",
    "token" : "* First call: If the parameter is empty or not provided, the system generates and returns a temporary token; * Second call: Required, pass in the token returned from the first call; the system will validate it and decide whether to accept the release operation based on the result"
  }
}