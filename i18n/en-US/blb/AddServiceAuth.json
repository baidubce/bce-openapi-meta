{
  "AddServiceAuth" : {
    "clientToken" : "Idempotency token, an ASCII string with a maximum length of 64 characters",
    "service" : "Domain name of the service publication endpoint",
    "authList" : "List of user authorizations",
    "authList.uid" : "User ID; use \"*\" to represent all users",
    "authList.auth" : "Authentication method, valid values: allow/deny, indicating permit/deny respectively"
  }
}