{
  "AuthorizeEnterpriseSecurityGroupRule": {
    "desc": "Add rules to an enterprise security group, up to 20 per request",
    "enterpriseSecurityGroupId": "ID of the target enterprise security group",
    "clientToken": "Idempotent token, max 64 ASCII characters",
    "rules": "List of rules to authorize",
    "rules.remark": "Rule remark, length 1-255",
    "rules.direction": "Rule direction: ingress or egress",
    "rules.ethertype": "Network type: IPv4 or IPv6 (default IPv4)",
    "rules.portRange": "Destination port range, e.g. 1-80 or 80,443 (default 1-65535)",
    "rules.sourcePortRange": "Source port range, e.g. 1-80 or 80,443",
    "rules.protocol": "Protocol: tcp / udp / icmp (default all)",
    "rules.sourceIp": "Source IP address, 'all' means any (ingress rules)",
    "rules.destIp": "Destination IP address, 'all' means any (egress rules)",
    "rules.localIp": "Local IP address, 'all' means any",
    "rules.remoteIpSet": "Remote IP address group (mutually exclusive with sourceIp/destIp and remoteIpGroup)",
    "rules.remoteIpGroup": "Remote IP address family (mutually exclusive with sourceIp/destIp and remoteIpSet)",
    "rules.action": "Action: allow or deny",
    "rules.priority": "Priority, range 1-1000"
  }
}
