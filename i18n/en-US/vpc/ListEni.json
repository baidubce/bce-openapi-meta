{
  "ListEni": {
    "desc": "List Elastic Network Interfaces",
    "vpcId": "VPC ID the ENIs belong to",
    "instanceId": "Filter by attached instance ID",
    "name": "Filter by ENI name",
    "privateIpAddress": "Filter by private IP; multiple IPs comma-separated (single IP: suffix match, multiple IPs: exact match)",
    "marker": "Pagination start marker",
    "maxKeys": "Maximum results per page, up to 1000, default 1000"
  }
}
